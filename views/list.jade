extends layout

block content
  div
    table.table
      thead
        tr.head
          th Start Time
          th To
          th Duration
          th Audio
      tbody
        each recording in user.recordings
          tr
            td
              !{moment(recording.statTime).format('MM/DD/YYYY h:mm a')}
            td
              !{recording.numberCalledFormatted}
            td
              !{moment.utc(recording.duration*1000).format('mm:ss')}
            td
              audio(controls='')
                source(src=recording.recordingUrl, type='audio/wav')
